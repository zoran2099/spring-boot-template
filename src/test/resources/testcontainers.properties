# Disable Ryuk (resource reaper container) — required for Docker daemons that
# do not expose their internal port mapping cleanly to the host (e.g. Rancher
# Desktop, remote Docker, some corporate proxies). Containers will still be
# stopped by the JVM shutdown hook.
ryuk.disabled=true
